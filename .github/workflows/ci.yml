name: CI

# Every push to main and every PR, on all three platforms. release.yml only runs on a v*
# tag and never runs the test suite, so before this nothing ever ran `./gradlew build`
# anywhere but a developer's machine. The shared contract fixture
# (src/test/resources/clipboard-contract.json, byte-identical in the VS Code repo) must
# pass on Windows, macOS and Linux — a payload is meant to restore identically on all of them.
on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
      - uses: gradle/actions/setup-gradle@v4
      # The copy actions put text on the AWT clipboard, which throws HeadlessException
      # without a display — a CI artefact, since a real user always has one. A virtual
      # display on Linux, exactly as the VS Code e2e job does; Windows and macOS runners
      # already have a desktop session.
      - name: Build and run the full test suite (Linux, virtual display)
        if: runner.os == 'Linux'
        run: xvfb-run -a ./gradlew build
      - name: Build and run the full test suite
        if: runner.os != 'Linux'
        shell: bash
        run: ./gradlew build
      - name: Keep test reports
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: test-reports-${{ matrix.os }}
          path: build/reports/tests/
          if-no-files-found: ignore
